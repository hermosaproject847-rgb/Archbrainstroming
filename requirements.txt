# SketchToPlan web build — server-side Python deps (Linux host).
# pywebview is NOT needed here (that is only for the desktop shell).
bottle
shapely
ezdxf
matplotlib
pillow
pypdfium2
pdfplumber
pymupdf
